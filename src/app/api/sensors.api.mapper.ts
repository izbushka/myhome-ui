import {SensorsApiResponse} from '@entities/sensors.interfaces';

export class SensorsApiMapper {
	public static mapSensors(data: SensorsApiResponse): SensorsApiResponse {
		// TODO: add icons and so on
		return data;
	}
}
